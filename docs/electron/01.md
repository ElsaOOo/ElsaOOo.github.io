---
lang: zh-CN
description: "Electron, Electron 安装"
tags:
  - electron 安装
---

# Electron 安装

**文章引用请注明出处**

Electron 使用纯Javascript 调用丰富的原生(操作系统)APIs来创造桌面端应用。

初始化一个electron应用很简单, 首先确保电脑中安装了node ， node 版本在10以上。

一个最基本的electron 应用目录

```markdown
your-app/
├── package.json
├── main.js
└── index.html
```

在package.json里面设置

```json
{
  "name": "your-app",
  "version": "0.1.0",
  "main": "main.js",
  "scripts": {
    "start": "electron ."
  }
}

```

这里要下载electron 包，可以在工程目录中下载，也可以全局下载。

```shell
npm install --save-dev electron
```

安装这一步，容易发生Error。比如electron这个包一直下载失败

在mac 环境下，打开 `~/.npmrc` 这个文件，修改以下配置：

```markdown
registry= https://registry.npm.taobao.org
electron_mirror="https://npm.taobao.org/mirrors/electron/"
```

然后再执行install 命令，就可以成功安装。