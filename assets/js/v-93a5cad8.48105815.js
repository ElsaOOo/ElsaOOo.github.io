(self.webpackChunkmy_blog_next=self.webpackChunkmy_blog_next||[]).push([[122],{3218:(e,t,a)=>{"use strict";a.r(t),a.d(t,{data:()=>r});const r={key:"v-93a5cad8",path:"/react-source-code/ref-lazy.html",title:"React.createRef",lang:"zh-CN",frontmatter:{lang:"zh-CN",description:"react 源码解析，react creatRef react lazy"},excerpt:"",headers:[{level:2,title:"React.lazy",slug:"react-lazy",children:[]}]}},5986:(e,t,a)=>{"use strict";a.r(t),a.d(t,{default:()=>n});const r=(0,a(6252).uE)('<h1 id="react-createref" tabindex="-1"><a class="header-anchor" href="#react-createref" aria-hidden="true">#</a> React.createRef</h1><p>这个顶层 API 是帮助我们获取到真实的 dom 元素，它的简单用法：在构造组件时，通常将 Refs 分配给实例属性，这样在整个组件中引用它们。</p><div class="language-react ext-react"><pre class="language-react"><code>class MyInput extends PureComponent {\n  constructor(props) {\n    super(props);\n    this.inputRef = React.createRef();\n  }\n\n  return (\n  \t&lt;input ref={inputRef} /&gt;\n  )\n}\n</code></pre></div><p>这样在这个组件 didMount 的时候，我们通过 inputRef.current 就能拿到这个 input 元素的真实 dom 节点了。</p><p>它的源码也很简单：</p><div class="language-javascript ext-js"><pre class="language-javascript"><code>// an immutable object with a single mutable value\nexport function createRef(): RefObject {\n  const refObject = {\n    current: null,\n  };\n  if (__DEV__) {\n    Object.seal(refObject);\n  }\n  return refObject;\n}\n</code></pre></div><p>作者源码注释写的是这个函数返回一个不变的对象，这个对象有一个可变的 current 属性。</p><p>React.createRef 是用在类组件里面的，<strong>不能在函数组件上使用 <code>ref</code> 属性，因为它们没有实例</strong>。</p><p>不能在函数组件上使用 ref 的意思是：</p><div class="language-react ext-react"><pre class="language-react"><code>class Demo extends React.Component {\n  constructor(props) {\n    super(props);\n    this.childRef = React.createRef();\n  }\n\n  return (\n  \t// 这样在函数组件上使用createRef是没有作用的\n  \t&lt;Child ref={this.childRef} /&gt;\n  )\n\n}\n\nconst Child = () =&gt; {\n  return (\n  \t&lt;input type=&quot;text&quot; /&gt;\n  )\n}\n</code></pre></div><p>如果要在函数组件上使用<code>ref</code> ，可以使用<code>forwardRef</code> ，或者将该函数组件转换成 class 组件。</p><p>在 hooks 中，<code>useRef</code>的函数签名是：</p><div class="language-javascript ext-js"><pre class="language-javascript"><code>useRef&lt;T&gt;(initialValue: T): {current: T}\n</code></pre></div><p>和 createRef 的返回值一样，只不过这里多了一个参数，但是这个参数又没有用到。</p><h2 id="react-lazy" tabindex="-1"><a class="header-anchor" href="#react-lazy" aria-hidden="true">#</a> React.lazy</h2><div class="language-javascript ext-js"><pre class="language-javascript"><code>// lazy接受一个函数，这个函数动态调用import()。返回一个Promise，该Promise需要resolve一个default export的React组件\nexport function lazy&lt;T, R&gt;(ctor: () =&gt; Thenable&lt;T, R&gt;): LazyComponent&lt;T&gt; {\n  let lazyType = {\n    $$typeof: REACT_LAZY_TYPE,\n    _ctor: ctor,\n    // React uses these fields to store the result.\n    _status: -1,\n    _result: null,\n  };\n\n  return lazyType;\n}\n</code></pre></div><p>🌰：</p><div class="language-react ext-react"><pre class="language-react"><code>const OtherComponent = React.lazy(() =&gt; import(&#39;./OtherComponent&#39;));\n</code></pre></div>',18),n={render:function(e,t){return r}}}}]);